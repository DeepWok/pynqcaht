#ifndef _MERGE_H_
#define _MERGE_H_

enum merge_regs {
    // Controller registers
    MERGE_CTRL_REG = 0x0,
    MERGE_STATUS_REG = 0x4,
    MERGE_RESULT_REG = 0x8,
    MERGE_1_REG = 0xC,
    MERGE_2_REG = 0x10
};

#endif